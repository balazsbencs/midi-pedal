# HIL fixture contract

The fixture must expose isolated TRS MIDI capture, USB-MIDI capture, relay
continuity probes, expression-pedal sweep input, a controllable power source,
and a display timing/photo trigger. Every manual operation starts unpowered;
the operator records instrument, range, probe points, board revision, and raw
evidence path. A simulated run cannot qualify relay, power, or enclosure safety.

