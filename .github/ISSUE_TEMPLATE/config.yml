name: Configuration or protocol issue
description: Report validation, import/export, sync, or compatibility behavior
title: "config: "
body:
  - type: textarea
    id: document
    attributes:
      label: Minimal JSON/configuration
      description: Attach a redacted JSON export and schema/image/protocol versions.
    validations:
      required: true
  - type: textarea
    id: result
    attributes:
      label: Expected and observed result
    validations:
      required: true

